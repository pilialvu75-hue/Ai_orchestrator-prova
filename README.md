# AIrLab

AIrLab is the model-runtime service for AI-Orchestrator's Cantiere.

This repository starts hardware-independent: the first milestone validates contracts, routing and end-to-end integration with a deterministic mock engine before any real LLM is added.
