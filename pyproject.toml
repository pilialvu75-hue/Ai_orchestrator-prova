[project]
name = "airlab"
version = "0.1.0"
description = "Hardware-independent builder runtime for AI-Orchestrator Cantiere"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "workers-py",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
